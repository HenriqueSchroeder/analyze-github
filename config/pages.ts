/**
 * Defines pages that don't require authentication.
 */

export const pages = {
  signIn: '/'
}
export const pagesWithoutAuthentication = [pages.signIn, '/favicon.ico']

/**
 * Static File Pages.
 */
export const staticPages = ['/_next']
